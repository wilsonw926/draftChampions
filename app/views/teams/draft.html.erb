Round <%= @round %> / 15 <br>
Team Overall: <%= @team.overall_sum/@team.players_array.length %><br>

<% @three_players.each do |player| %>
	Name: <%= player.name %>
	Postion: <%= player.position %>
	Overall: <%= player.overall %>
	<%= image_tag player.front_card.variant(resize: '200x200') %>
	<%= image_tag player.back_card.variant(resize: '200x200') %><br>
	<%= button_to "Draft Player", add_player_path(id: @team.id, round_number: @round, selected_player: player.id, player_overall: player.overall) %>
<% end %>

<%= render '/teams/draft_partials/draft_current_team' %>
